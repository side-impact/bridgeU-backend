# 로컬 개발 환경용 설정 (민감 정보 포함)
# 이 파일은 Git에 커밋하지 않습니다

# PostgreSQL 데이터베이스 연결 설정 (로컬)
spring.datasource.url=jdbc:postgresql://localhost:5432/bridgeu
spring.datasource.username=postgres
spring.datasource.password=1234
spring.datasource.driver-class-name=org.postgresql.Driver

# Papago API 설정 (로컬 개발용)
papago.client-id=dp4t2pl4a4
papago.client-secret=3rKxO310EldryUU0S4gMiamlppCIQtmAxjyr320W

# HMAC 시크릿 (로컬 개발용)
translator.hmac-secret=dev-secret-change-me

# SQL 초기화 모드 - 로컬에서는 항상 실행
spring.sql.init.mode=always

# 테스트 데이터 포함 - 로컬에서만
# 이 설정으로 testdata.sql이 로컬에서만 실행됩니다
# 프로덕션에서는 이 변수가 없어서 testdata.sql이 제외됩니다

# 로그 레벨 설정 (디버깅용)
logging.level.com.example.demo=DEBUG
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver=DEBUG
logging.level.org.springframework.web.servlet.handler.SimpleMappingExceptionResolver=DEBUG
logging.level.root=INFO
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n

# 한글 인코딩 설정
server.servlet.encoding.charset=UTF-8
server.servlet.encoding.enabled=true
server.servlet.encoding.force=true
